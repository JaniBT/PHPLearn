<?php
    $username = "Jani The BT";
    $phone = "123-456-7890";

    // $username = strtolower($username); -- LowerCase
    // $username = strtoupper($username); -- UpperCase
    // $username = trim($username);
    // $username = str_pad($username, 20, "0"); -- Kiegészíteni a stringet valamennyi karakterig valamilyen karakterrel
    // $phone = str_replace("-", "", $phone);
    // $username = strrev($username); -- string visszafele
    // $username = str_shuffle($username); -- Össze keveri a string karaktereit
    // $equals = strcmp($username, "Jani BT"); -- String összehasonlító
    // $count = strlen($username); -- string hosszúság
    // $index = strpos($username, " "); -- string-ben karakter keresés
    // $firstname = substr($username, 0, 4); -- string-ből karakterek kiszedése megadott indextől indexig.
    // $fullname = explode(" ", $username); -- string-ből tömb készítése egy megadott szeparátorral.

    // $username = implode("-", $username); -- Tömbböl egy darab string készítése megadott szeparátorral, a különböző értékek között

    // foreach ($fullname as $name) {
    //     var_dump($name);
    // } 
    // echo $fullname;
?>